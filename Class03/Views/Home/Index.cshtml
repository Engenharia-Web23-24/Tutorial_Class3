@model IEnumerable<Class03.Models.FileViewModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Upload">Submit New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Size)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Size)
            </td>
            <td>
                <a asp-action="Download" asp-route-id="@item.Name" target="_blank">Download</a> |
                <a asp-action="Delete" asp-route-id="@item.Name">Delete</a>
                @* @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ }) *@
            </td>
        </tr>
}
    </tbody>
</table>

<div>Number of files : @ViewBag.quantosFicheiros</div>
@* <div>Number of files : @ViewData["quantosFicheiros"]</div> *@
<div>Total bytes : @ViewBag.quantosBytes</div>
@* <div>Total bytes : @ViewData["quantosBytes"]</div> *@